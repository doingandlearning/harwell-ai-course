package com.example.library.service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

/**
 * Demo 4 (Unit testing): Generate tests here with AI.
 * Use prompts from prompts/04-unit-testing-prompts.txt.
 * Target: BookService.findBooksByAuthor (happy path, null, empty string, edge cases).
 */
@ExtendWith(MockitoExtension.class)
class BookServiceTest {

    // Add @Mock BookRepository, @InjectMocks BookService, and test methods after generating with AI
}
